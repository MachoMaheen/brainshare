import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs/promises";import os from "node:os";import path from "node:path";import { initProject,loadProject,compileProjectSlice,writeJson } from "./lib.mjs";
test("initializes, identifies, and compiles a local project",async()=>{const root=await fs.mkdtemp(path.join(os.tmpdir(),"bs-"));await fs.writeFile(path.join(root,"README.md"),"# Home\n[[API]]");await fs.mkdir(path.join(root,"docs"));await fs.writeFile(path.join(root,"docs","API.md"),"# API");await initProject(root,"demo");const sf=path.join(root,".brainshare","slices.json");await writeJson(sf,{version:1,slices:{demo:{id:"demo",title:"Demo",include:["README.md","docs/*.md"]}}});const p=await loadProject(root);assert.equal(Object.keys(p.manifest.notes).length,2);const{slice}=await compileProjectSlice(root,"demo");assert.equal(slice.files.length,2);assert.equal(slice.edges.length,1)});
