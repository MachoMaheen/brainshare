import test from "node:test"; import assert from "node:assert/strict"; import { BrainSharePublisherClient } from "../dist/index.js";
test("publishes the compiled slice through existing wrapper API",async()=>{let seen;const fake=async(u,o)=>{seen={u,o};return new Response(JSON.stringify({url:"https://x/share/a"}),{status:200})};const c=new BrainSharePublisherClient({baseUrl:"https://pub.example",token:"t",fetchImpl:fake});await c.publishSlice({protocol:1,id:"a",revision:"r",title:"A",description:"",visibility:"unlisted",live:true,createdAt:new Date(0).toISOString(),files:[{id:"01JQ8Y4B9G0HBRQKCR3T6G9QJ0",path:"a.md",hash:"h",title:"A"}],edges:[],backlinks:{}});assert.match(seen.u,/wrappers\/a$/);assert.equal(JSON.parse(seen.o.body).ulids.length,1)});
