{
  "name": "brainshare-publisher",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that stores notes in KV and renders /:ulid as HTML",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "marked": "^11.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240403.0",
    "typescript": "^5.3.3",
    "vitest": "^1.6.0",
    "wrangler": "^3.50.0"
  }
}
