const filesEl=document.querySelector('#files'),recentEl=document.querySelector('#recent'),doc=document.querySelector('#doc'),pathEl=document.querySelector('#path');let data,active,raw=false;
async function loadIndex(){data=await fetch('/api/files').then(r=>r.json());filesEl.innerHTML='';for(const p of data.files){const row=document.createElement('label');row.className='file';row.dataset.path=p;row.innerHTML=`<input type="checkbox" value="${esc(p)}"><span>▱</span><span>${esc(p)}</span>`;row.querySelector('span:last-child').onclick=e=>{e.preventDefault();openFile(p)};filesEl.append(row)}recentEl.innerHTML=(data.recent||[]).slice(0,6).map(x=>`<div class="recent"><b>${esc(x.path)}</b><br>${new Date(x.at).toLocaleTimeString()}</div>`).join('')||'<div class="recent">Watching for agent output…</div>';const requested=new URL(location).searchParams.get('file')||data.initial;if(requested&&!active)openFile(requested)}
async function openFile(p){const r=await fetch('/api/file?path='+encodeURIComponent(p)).then(r=>r.json());if(r.error)return;active=p;pathEl.textContent=p;document.querySelectorAll('.file').forEach(x=>x.classList.toggle('active',x.dataset.path===p));doc.innerHTML=raw?`<pre><code>${esc(r.raw)}</code></pre>`:r.html;doc.querySelectorAll('.wikilink').forEach(x=>x.onclick=()=>{const t=x.dataset.target.toLowerCase();const hit=data.files.find(p=>p.replace(/\.md$/i,'').split('/').pop().toLowerCase()===t);if(hit)openFile(hit)})}
document.querySelector('#raw').onclick=()=>{raw=!raw;if(active)openFile(active)};document.querySelector('#slice').onclick=async()=>{const selected=[...document.querySelectorAll('.file input:checked')].map(x=>x.value);if(!selected.length)return alert('Select Markdown files first.');const title=prompt('Slice title','Context');if(!title)return;const id=(prompt('Slice id',title.toLowerCase().replace(/[^a-z0-9_-]+/g,'-'))||'').trim();if(!id)return;const r=await fetch('/api/slice',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({id,title,files:selected})}).then(r=>r.json());alert(r.ok?`Created Live Slice “${title}” in .brainshare/slices.json`:r.error)};
new EventSource('/events').addEventListener('change',async e=>{const c=JSON.parse(e.data);await loadIndex();if(c.path===active)openFile(active)});function esc(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}loadIndex();
